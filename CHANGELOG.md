# Changelog

All notable changes per phase. Conventional-commit style; entries land alongside the PR that introduces them.

## Phase 1 — Metadata Core

### Unreleased

- **TASK-01 · Monorepo Scaffold** — empty monorepo with tooling wired up, CLAUDE.md + RFC-001 committed, `pnpm verify` runs a green smoke test per package, CI runs `pnpm verify` on every push and PR.
- **TASK-02 · Local Dev Environment** — `infra/docker/compose.dev.yml` runs Postgres 16, Redis 7, OpenSearch 2 (warm ~10 s, healthy via `--wait`); `.env.example` documents every variable the stack and future services read; `docs/runbooks/local-dev.md` covers start/inspect/reset/teardown with verified commands; Testcontainers smoke suite in `packages/db` exercises all three services (6 tests / ~10 s); `pnpm test:integration` split from `pnpm test` so integration tests stay off the every-commit verify loop.
- **TASK-03 · Metadata Schema** — four RFC §4.1 tables (`metadata.meta_object`, `meta_change_set`, `meta_layer_activation`, `meta_audit_log`) with Kysely types, the `idx_meta_object_current` partial index, and RLS policies (ENABLED + FORCEd). A non-superuser `erp_app` role + `withTenantContext(db, tenantId, fn)` API together make policies actually fire in dev. SQL-file migration runner via Kysely's `Migrator` (idempotent, re-run-safe) plus a `pnpm db:migrate` CLI. Integration tests prove cross-tenant reads are blocked and the migrator round-trips cleanly. [ADR-0001](./docs/adr/0001-metadata-schema.md) records the non-DDL decisions; `docs/patterns/writing-a-migration.md` now has the real example.
- **TASK-04 · Core Domain Types** — `@erp/core` now exports Zod schemas with inferred TypeScript types for every metadata object type from RFC §2: `Layer`, `ObjectType`, `ObjectId`, `Locale`, `LocalizedString`, `LocalizationBody`, 15-variant discriminated `Field`, `Relationship`, `EntityBody` (with `.superRefine` rules for duplicate field names and money-field → currency-field reference), `PermissionBody`, plus the operation-discriminated `Envelope` (upsert | tombstone) and `envelopeWithBody<T>` helper for strongly-typed wrappers. 158 tests — every schema has a round-trip test proving `parse → JSON.stringify → JSON.parse → parse` is the identity function. `@erp/core`'s only production dep stays `zod`. `docs/patterns/writing-a-domain-type.md` populated with the real `LocalizedString`/`EntityBody` examples.
- **TASK-06 · Resolution Algorithm** — `@erp/metadata` now ships `resolve()` matching RFC §3.2 pseudocode exactly: walks layers bottom-up, deep-clones the first body, merges subsequent bodies per the candidate's strategy, resets effective + provenance on tombstone. Returns `Result<ResolvedObject<T>, { kind: "object_not_found"; ... }>`. Four pure merge strategies (`replace`, `merge_object`, `append`, `merge_list_by_key`) plus a `merge()` dispatcher. Zero I/O — the `MetadataStore` port lives in `@erp/core/src/ports` with `LayerCandidate` and `MergeStrategy` types. Property tests with fast-check verify determinism, tombstone correctness, and ordered layer application (100 runs each). Benchmark test asserts p99 < 0.5 ms for a 30-field × 3-layer resolve (measured: p99 ≈ 0.3 ms, p50 ≈ 0.08 ms). 38 metadata tests total.
- **TASK-05 · Result Type and EventBus Port** — `@erp/core` now exports `Result<T, E>` (plain discriminated-union data + namespace object with `ok`, `err`, `isOk`, `isErr`, `map`, `mapErr`, `flatMap`, `match`, `unwrap`, `unwrapOr`, `unwrapOrElse`) plus the `EventBus` port: Zod `DomainEventSchema` with UUID v4 event_id, dotted-lowercase event_type, W3C `TraceContext`, nullable tenant_id/actor_id, dedup_key, and a generic `DomainEvent<TPayload>` shape. `publish`/`subscribe`/`waitFor` interface + `Subscription` shape. 200 tests across `@erp/core` (+42 this task). `docs/patterns/handling-errors.md` populated with the real Result example, combinator table, HTTP-edge conversion, and the three-and-only-three cases where `throw` is correct.
- **TASK-07 · Change Set State Machine** — RFC §9.3 lifecycle implemented across two packages. `@erp/change-set` ships the pure pieces: state machine (`transition(from, action, actor)` returning `Result<NextState, TransitionError>` with role guards `metadata.write` / `metadata.approve` / `metadata.deploy`), Zod-discriminated `Operation` schemas (`upsert` / `tombstone`), and `buildChangeSetEvent()` factories per transition. `@erp/db` ships `ChangeSetRepository` extending the new `TenantRepository` base — atomic deploy materializes staged operations into `meta_object` rows inside one transaction (CHECK violation mid-batch rolls back every row), O(1) rollback via `superseded_by_change_set_id` (two `UPDATE`s bounded by the rollback's change_set_id). Migration 0002 adds `staged_operations jsonb` and the `superseded_by_change_set_id text` column. Audit entries write inside the same transaction as the data they audit. 20 integration tests in `@erp/db` (was 15) cover approve-then-deploy, deploy-then-rollback, and "failed deploy leaves system consistent". 58 unit tests in `@erp/change-set`.
- **TASK-08 · In-Process EventBus + Outbox** — `@erp/events` ships `OutboxBus` (implements the `EventBus` port from `@erp/core`) backed by `metadata.meta_outbox` (Migration 0003: UNIQUE on event_id + dedup_key, partial index on pending rows, RLS ENABLED + FORCED). Two publish surfaces: `publish()` (own tx) and `publishWithin(trx, event)` (atomic with caller's tx — the outbox-with-data pattern). `OutboxPump.drainOnce()` reads pending rows with `FOR UPDATE SKIP LOCKED`, dispatches via the bus, flips delivered_at on success / increments attempt_count on throw. Dead-letter cap at 8 attempts via `pump.deadLettered()`. `start()` / `stop()` for the polling loop with adaptive backoff. `apps/worker` exposes `createWorker(config)` that constructs DB + bus + pump in one wiring location per CLAUDE.md §7 #11. 7 integration tests cover atomicity, dedup_key collapse, pump delivery, restart durability, failing-handler retry, and per-type fan-out. `docs/patterns/emitting-an-event.md` populated with the two publish surfaces, idempotency rules, and pump internals.
- **TASK-09 · Fastify API Skeleton** — `apps/api` boots a Fastify 4 server via `buildServer()` (the single wiring point per CLAUDE.md §7 #11). Five plugins layered: telemetry (pino child + x-request-id + W3C trace*id), errors (RFC 7807 problem+json for ZodError, FST_ERR_VALIDATION, statusCode passthrough, 404 not-found), auth (placeholder for Better Auth — accepts x-user-id / x-user-roles in dev; full auth lands in TASK-10), tenant-context (validates x-tenant-id against `t*[a-z0-9_]{2,62}`, child-binds the logger), openapi (serves `/docs/openapi.json`from the registry every route registers against). Routes shipped:`/healthz`, `/readyz`(DB ping),`/docs/openapi.json`. Zod helpers `parseBody/parseQuery/parseParams`(Fastify 4 doesn't natively understand Zod; type-provider-zod requires Fastify 5). 12 contract tests via`fastify.inject()`cover every plugin path. Deps added to apps/api:`fastify@4.29.0`, `fastify-plugin@5.0.1`, `pino@catalog`, `@asteasolutions/zod-to-openapi@7.3.0`. `@erp/telemetry`ships`createLogger()`with structured fields + redaction (5 unit tests).`@erp/config/eslintrc.cjs`grows a`**/plugins/**/\*.ts`override so Fastify's`export default fp(...)`idiom keeps working without dropping no-default-export elsewhere.`docs/patterns/writing-a-route.md`populated with the canonical`/healthz` example + a tenant-scoped CRUD shape.
- **TASK-10 · Admin API for Metadata CRUD** — All 9 RFC §9.1 endpoints in `apps/api/src/routes/admin/`. Read-side: `GET /admin/v1/metadata/objects[?type=&layer=]`, `GET /admin/v1/metadata/objects/{id}` (resolved through `@erp/metadata`), `GET /admin/v1/metadata/objects/{id}/history` (tenant + vendor unioned). Write-side, role-gated via a new `requireRole(name)` preHandler factory: `POST /admin/v1/metadata/changes` (`metadata.write`), `/{id}/{simulate,propose}` (`metadata.write`), `/approve` (`metadata.approve`), `/{deploy,rollback}` (`metadata.deploy`). `@erp/db` ships `MetadataObjectRepository` extending `TenantRepository` and implementing the `MetadataStore` port (the resolver reads through it). Two thin services (`MetadataObjectService`, `ChangeSetService`) return `Result<T, ServiceError>`; routes Result.match into RFC 7807 problem+json with exhaustive error mapping. `@fastify/rate-limit` configured globally with per-tenant keys + per-route overrides on writes (60/min; simulate 30/min). `apps/api/src/openapi-registry.ts` calls `extendZodWithOpenApi(z)` so the generator can introspect parameter schemas. 16 contract tests via `fastify.inject()` cover the full lifecycle (propose → approve → deploy → rollback round-trips through the resolver). `docs/patterns/writing-a-service.md` populated with the real ChangeSetService example. **Better Auth deferred** — placeholder `x-user-id` / `x-user-roles` auth from TASK-09 gates role checks until TASK-10.1 lands. See [ADR-0002](./docs/adr/0002-better-auth-zod-4-deferral.md) for the Zod 4 migration plan.
- **TASK-11 · Application Kernel Service** — `apps/kernel` boots a standalone Fastify 4 service via `buildKernel()` (the single wiring point; CLAUDE.md §7 #11). `KernelCache` = in-process L1 Map-based LRU + optional ioredis L2 (keys `erp:kernel:<tenant>::<objectId>`, 1 h TTL), with a prefix-SCAN tenant sweep on invalidation and graceful degradation when Redis is unreachable. `CacheInvalidator` is a read-only outbox poller — initializes its cursor to `max(outbox_pk)` at boot, ticks every 250 ms, consumes only `metadata.change_set_deployed` events, and calls `cache.invalidateTenant(row.tenant_id)`. Each kernel instance runs its own; no cross-kernel coordination. The worker app's `OutboxPump` still owns flipping `delivered_at`. `ResolveService` is the hot path: probe cache → on miss, call `@erp/metadata.resolve()` through `MetadataObjectRepository` (which implements `MetadataStore`), store in both tiers, log `{event: "resolve", tenant_id, object_id, cache_status, duration_ms}`. Every resolve opens an OTel span (no-op until an SDK registers) — `@erp/telemetry` now exports `createTracer(scope)` backed by `@opentelemetry/api`. `POST /internal/resolve` returns `{object_id, body, provenance, cache_status, duration_ms}`. Plus `/healthz`, `/readyz`, `/docs/openapi.json` mirroring apps/api. 10 integration tests cover the contract, the two-kernel fleet-invalidation SLO (RFC §5.4 — both kernels' caches evict after a deploy event lands in the shared outbox), tenant isolation of invalidation, cursor monotonicity, and non-deploy events being ignored.
- **TASK-12 · Runtime API Auto-Derivation** — RFC §9.2 CRUD endpoints auto-derived from deployed Entity metadata. Migration 0004 adds the `ops` schema + `ops.entity_row(tenant_id, entity_id, row_id uuid, body jsonb, status, created_at, updated_at, deleted_at)` with strict-tenant RLS (no vendor escape hatch for ops data). `@erp/db` ships `EntityRowRepository extends TenantRepository` with `list/get/create/patch/softDelete` + a `createInTx` variant for callers that already hold a tenant-bound transaction (13 integration tests). `@erp/kernel-runtime` populates: `zodFromField(field)` translates every one of the 15 Field variants to a Zod validator; `materialize(resolved)` returns `{entity, fieldsByName, createValidator, patchValidator}` (strict keys, required-field enforcement on create, full-optional on patch); `MaterializedEntityCache` is a version-keyed LRU (24 unit tests). Apps/api adds: `PermissionGate` (RFC §13.1 level 1 — role-based entity-level grants, inheritance via `inherits_from`, deny-by-default), `RuntimeEntityService` (resolve → materialize → gate → validate → repository, returns `Result<T, RuntimeError>`), and `GET/POST/PATCH/DELETE /v1/:entity[/:id]` routes. Cache key hashes the full provenance stack so adding an L2 layer for a tenant rotates the key even when every layer's version is 1. Storage strategies `jsonb` and `hybrid` ship in Phase 1; `native` and `side_table` return 501 (CLAUDE.md §6 — deferred to when the Migration Primitives land). Permission Gate denies before leaking entity existence — an unknown entity to an unauthorized caller returns 403, not 404. 18 new integration tests cover CRUD contract, permission gate levels (deny-by-default, no-matching-role, action-not-granted), OpenAPI registration, and the CLAUDE.md §13 E2E: deploy ent.customer at L0 → deploy a Change Set adding loyalty_tier at L2 → POST a customer with the new field → GET it back — all without a process restart or redeploy. `apps/api/src/server.ts` wires the new service + gate + cache in the single buildServer() call. `MetadataObjectRepository.listActiveObjectIds(tenantId, type)` added for the gate to enumerate tenant-visible `prm.*` ids (vendor + tenant-scoped union).
- **TASK-13 · Reference Tenant Seed** — `pnpm db:seed` now populates `t_demo_retail` in three idempotent phases. Phase 1 (vendor L0): `ent.customer`, `ent.product`, `ent.invoice` Entities + `prm.admin` + `prm.viewer` Permissions, all with Arabic + English labels. Phase 2 (tenant L2 for `t_demo_retail`): Customer gets a `loyalty_tier` enum added at the tenant layer — demonstrates the five-layer customization model with real data. Phase 3 (rows): 50 Customers (deterministic mixed-locale names + phones + cities), 50 Products (localized_string names in Arabic and English across grocery/electronics/apparel/household), 50 Invoices linking back to the seeded Customers. Each phase is idempotent: `cs_seed_platform_v1` and `cs_seed_demo_retail_v1` marker Change Sets gate the metadata phases; row inserts use deterministic sha1-derived UUIDs + `ON CONFLICT (tenant_id, entity_id, row_id) DO NOTHING` for row-level idempotency. `scripts/seed.ts` uses `set_config('app.current_tenant', $1, true)` (the parameterizable form) rather than `SET LOCAL` so tenant ids flow through placeholders. 8 integration tests run the seeder against Testcontainers Postgres: first pass inserts exactly {5 vendor objects, 1 tenant object, 3 × 50 rows}; second pass is a strict no-op; partial-completion recovery (vendor phase succeeded but tenant phase didn't) fills only the missing phases; the Runtime API serves all 50 Customers with loyalty_tier populated, 50 Products with Arabic + English labels, 50 Invoices each referencing a seeded customer; `prm.viewer` can read but not create. Also added `packages/config/tsconfig.json` (shim extending the base) so vite/vitest's tsconfck resolver doesn't error when a test imports from `scripts/`.
- **Architectural decision records** — [ADR-0002](./docs/adr/0002-better-auth-zod-4-deferral.md) documents why Better Auth integration was deferred through TASK-10 and TASK-12 (peer-depends on Zod 4 while `@erp/core` is on Zod 3), and pins the Zod 3 → Zod 4 migration path for a follow-up TASK-10.1. Three alternatives (pin Better Auth at 1.0.x, write minimal custom auth, swap Zod for Valibot/ArkType) considered and rejected with reasoning recorded.
- **TASK-10.1b.1 · Better Auth wiring (session layer, phase 1 of 2)** — the placeholder `x-user-id` / `x-user-roles` header auth now has a real Better Auth session layer running alongside it. `@erp/auth` package (new) wraps `better-auth@1.6.8` + `@better-auth/kysely-adapter@1.6.8` and exposes three entry points: `createAuth({db, secret, baseURL, isProduction})` builds the server instance against the shared Kysely via schema-qualified `modelName` overrides (`auth.user`, `auth.session`, `auth.account`, `auth.verification`) so one connection pool serves both the app and the auth tables; `resolveSession(auth, {headers})` + `resolveTenantContext(db, userId, tenantId)` do the per-request join against `metadata.user_tenant` to populate `appContext.{userId, userRoles}`; `createTestSession(db, auth, {tenantId, userId, email, roles})` is the fixture every integration test will migrate to — direct-insert rows into `auth.user` + `auth.session` + `metadata.user_tenant`, build the HMAC-SHA256-signed session cookie (standard base64 to match Better Auth's `makeSignature` exactly — the lib uses `btoa(String.fromCharCode(...))`, not base64url), return a cookie string ready for `fastify.inject({headers: {cookie}})`. `apps/api/src/plugins/auth.ts` rewritten: mounts Better Auth's Node handler at `/api/auth/*` via a Fastify catch-all that bridges to the Web Request/Response shape BA expects; on every other request, tries to resolve the session cookie first, falls back to dev headers (`NODE_ENV !== "production"` only, with a deprecation warning per hit), 401s when no session and `required: true`; validates session membership against the requested `x-tenant-id` and 403s cross-tenant attempts before leaking tenant existence. `apps/api/src/plugins/tenant-context.ts` now bypasses `/api/auth/*` since login flows don't know a tenant yet. 5 new integration tests in `apps/api/test/integration/auth.integration.test.ts` cover: the `createTestSession` fixture works end-to-end against an admin route; role-gated writes see the right `userRoles` (reader gets 403, writer gets 201); cross-tenant session attempts get 403; real `/api/auth/sign-up/email` → `/api/auth/sign-in/email` → authenticated admin call round-trips; no-session-no-header requests 401. All 69 existing api integration tests still pass (dev-header fallback unchanged). New [ADR-0004](./docs/adr/0004-better-auth-wiring.md) documents the decisions (shared Kysely, `modelName` overrides, per-request session resolution, dual-mode phasing); ADR-0002 marked Superseded. `apps/api` gains `@erp/auth` as a workspace dep. **Remaining for TASK-10.1b.2 (follow-up):** mechanical migration of every integration test off dev headers onto `createTestSession`, one PR per file; deletion of the dev-header fallback when the last file migrates; console login-form swap from the JSON dev cookie to Better Auth's `/api/auth/sign-in/email`. `pnpm verify` green.
- **TASK-14.5 · Production infra scaffold (Terraform + ECS Fargate + Dockerfiles + deploy workflow)** — full production path lands: four multi-stage Dockerfiles (api/kernel/worker via pnpm `deploy --prod --legacy` into alpine + tini + non-root; console via Next.js `output: "standalone"`), a complete `infra/terraform/` root module + `bootstrap/` sub-module, a deploy workflow, and a first-time-setup runbook. Terraform ships: `versions.tf` (AWS provider ~> 5.70, TF >= 1.9), `providers.tf` (Frankfurt default-tags), `backend.tf` (S3 + DynamoDB remote state with `-backend-config` placeholders), `variables.tf` + `locals.tf` (services map so the three ECS services iterate with one for_each), `vpc.tf` (10.40.0.0/16 with three AZs, public/private subnets, NAT per AZ, S3 gateway endpoint), `security-groups.tf` (per-role SGs, ingress declared separately so the reviewer sees the service graph at a glance), `rds.tf` (Postgres 16.6 GP3 autoscaling + force_ssl + pg_stat_statements), `elasticache.tf` (Redis 7.1 with at-rest + in-transit encryption, automatic failover when num_nodes > 1), `s3.tf` (attachments bucket with Block Public Access + versioning + lifecycle), `secrets.tf` (database_url + redis_url auto-populated, better_auth_secret + grafana_otlp_headers as empty placeholders operators populate via awscli), `ecr.tf` (four immutable-tag repos with lifecycle policy expiring untagged after 7 days + keeping last 50 tagged), `alb.tf` (public ALB for api with ACM cert conditional + HTTP→HTTPS redirect, internal ALB for kernel's internal/resolve RPC), `ecs.tf` (Fargate cluster + Container Insights + per-service task-execution role + per-service task role, task defs pulling images from ECR with SecretsManager-sourced secrets, services with `ignore_changes = [task_definition]` so the deploy workflow can rotate task-defs without Terraform fighting it), `outputs.tf` (URLs, ECR repos, cluster/service names, secret ARNs). Bootstrap module (local state) creates the S3 state bucket + DynamoDB lock table with `prevent_destroy = true`. `staging.tfvars` is the conservative-defaults file; `prod.tfvars.example` shows the prod upsizing. Deploy workflow (`.github/workflows/deploy.yml`): OIDC-authenticated, matrix-builds four images via Buildx + GHA cache, pushes to ECR with both `sha-<commit>` and `:latest` tags, runs migrations via the api image's runner, then forces ECS to redeploy each service via `amazon-ecs-deploy-task-definition` with `wait-for-service-stability`. Supports manual `workflow_dispatch` against staging/prod. `.github/workflows/terraform.yml` runs `terraform fmt -check -recursive` + `terraform init -backend=false` + `terraform validate` on every PR touching `infra/terraform/**`. `docs/runbooks/production-deploy.md` is the single operator onboarding doc — bootstrap → first apply → secret population → first deploy → DNS+TLS → OTLP wire-up → rollback procedure → post-deploy hardening checklist → staging teardown. `.dockerignore` keeps the build context tight (drops node_modules, .next, .turbo, docs, infra/terraform, test artifacts). `.gitignore` protects `*.tfstate`, `prod.tfvars`, `infra/terraform/.terraform/`. Next.js config flipped to `output: "standalone"` with `outputFileTracingRoot` pointing at the monorepo root so the traced bundle has every `@erp/*` workspace dep it needs. The staging-bring-up step ("single tenant pilot works end-to-end against a live AWS account") is the remaining operator task — it requires AWS credentials Claude doesn't have. The HCL + workflow + runbook cover every step the operator walks through; the terraform-validate CI job catches HCL regressions without needing an AWS account. `pnpm verify` green.
- **TASK-14.4 · Playwright E2E smoke** — `apps/console` now ships a real end-to-end harness via Playwright 1.59.1. `apps/console/playwright.config.ts` declares two projects — a `setup` project that visits `/login`, fills the dev form with the seeded tenant, and persists the resulting cookie jar to `test/e2e/.auth/session.json`, plus a `chromium` project that reuses that storage state so every scenario starts already signed-in. `globalSetup` pings `/readyz` and `/v1/ent.customer?limit=1` up front so the suite fails fast with a clear message if the upstream stack isn't listening or the seed hasn't run. The console is launched on port 3003 (separate from `pnpm dev`'s 3002) so the harness never collides with a developer's running session. Two scenarios: (1) Customer PATCH round-trip — opens the first seeded customer, flips `loyalty_tier` (the L2-overlay enum) to a new value, saves via the Server Action, reloads, asserts the value persisted in both the form and the Raw JSON panel; (2) Locale toggle + RTL — asserts `<html dir>` flips from `ltr` to `rtl`, the `lang` attribute moves to `ar`, and the Arabic "الكيانات" label renders in the top nav. Both scenarios run against the real stack (api + kernel + worker + Postgres + Redis) and complete in ~9 s on a warm machine. Every metadata-driven form input in the detail form now carries a `name` attribute (`<select name="loyalty_tier">`, `<input name="number">`, localized_string inputs named `{field}.en`/`{field}.ar`) — the form still POSTs a hidden `__body__` JSON blob, so the names are ignored by the submission path but give Playwright stable locators and improve accessibility. New `test:e2e` + `test:e2e:ui` scripts on `@erp/console`; `@playwright/test@1.59.1` as a devDep. Shared ESLint override allows default export in `**/test/**/global-setup.ts` (Playwright's convention). New CI workflow `.github/workflows/e2e.yml` runs on every push + PR: spins up Postgres 16 + Redis 7 via `services:`, applies migrations, seeds the demo tenant, launches api/kernel/worker in the background, polls `/readyz`, runs the Playwright suite, and uploads the HTML report + dev-services log as artifacts on failure. Turbo's `test:e2e` task depends on `build`, has no cache, and outputs `playwright-report/**` + `test-results/**`. `pnpm verify` green; the suite itself is scoped to the `test:e2e` run so it doesn't weigh down every-commit verify.
- **TASK-14.3 · Grafana Cloud OTLP exporter wiring** — `@erp/telemetry` now ships `registerOtelSdk({serviceName, endpoint, headers, ...})` + the env-gated `registerOtelSdkFromEnv(serviceName)`. Each service (`apps/api`, `apps/kernel`, `apps/worker`) calls the env-gated variant in its `src/index.ts` _before_ building its handle — when `GRAFANA_CLOUD_OTLP_ENDPOINT` is set, the full OTel `NodeSDK` starts with an OTLP/HTTP trace exporter and a `PeriodicExportingMetricReader` (60 s default, overridable) pointed at Grafana Cloud; when unset (dev + tests), the call is a no-op and the `@opentelemetry/api` NoOp tracer stays in place. Signal paths `/v1/traces` + `/v1/metrics` appended automatically so callers pass a single base URL. `GRAFANA_CLOUD_OTLP_HEADERS` parsing matches the OTel spec (comma-separated `key=value`, URL-decoded values — so Grafana's `authorization=Basic%20...` works as a shell variable). New `otelContextMixin` wired into every `createLogger()` instance injects `trace_id` + `span_id` from the active OTel span into every pino log line (zero-ids filtered), so correlated logs land even before the request-scoped child logger picks up the W3C traceparent. Six new OTel core deps (`sdk-node`, `sdk-trace-base`, `sdk-metrics`, `resources`, `semantic-conventions`, `context-async-hooks`, `exporter-trace-otlp-http`, `exporter-metrics-otlp-http`) added to `@erp/telemetry` only — no app-level churn. 16 unit tests (env-var parsing, NoOp handle, mixin context extraction + zero-id filtering) + 1 integration test: Fastify mock OTLP collector receives a span from `createTracer()`, round-trip through `NodeSDK.shutdown()`, assert on decoded OTLP-JSON payload (service.name, span name, attribute, scope name). `.env.example` documents both variables. Each entry point's shutdown path now `await`s `otel.shutdown()` so pending spans flush before exit. `pnpm verify` green; `pnpm --filter @erp/telemetry test:integration` passes in ~400 ms.
- **TASK-14.1 · Audit hash-chain unification + backfill** — `ChangeSetRepository.writeAudit()` now delegates to `AuditRepository.appendInTx()`, so every change-set transition (created/propose/approve/deploy/rollback) writes a hash-chained row atomic with its data. From this commit forward, Runtime API writes and Admin API change-set writes share one per-tenant chain. For the pre-existing un-chained rows (TASK-07-era), `scripts/backfill-audit-chain.ts` (`pnpm db:backfill-audit-chain`, `-- --dry` for a plan-only run) walks every audit row in `audit_pk` order and fills in `before_hash` + `after_hash`. Per-tenant chains never cross; vendor-level (`tenant_id IS NULL`) rows chain independently. One transaction per tenant so concurrent readers see either the old head or the fully rebuilt chain. Partial-state recovery: if some rows carry hashes and later ones don't, the script keeps the valid prefix and only rebuilds from the first mismatch. Idempotent: a clean run returns `rowsRewritten: 0`. 6 integration tests in `apps/api/test/integration/audit-backfill.integration.test.ts` cover pre-state, full rebuild, idempotency, vendor-chain isolation, partial-state recovery, and default-write behavior.
- **TASK-14.2 · Console "+ New" row UI** — `apps/console` gains a full create flow at `/entities/:entity/new`. Server Component fetches the resolved EntityBody via Admin API; `CreateEntityForm` (Client) renders one input per declared field with the shape derived from metadata (enum → select, integer/money → number with step, boolean → true/false select, localized_string → en + ar paired inputs, date/datetime → appropriate HTML5 types). Submit goes through a new `createRowAction(entityId, formData)` Server Action that POSTs to `/v1/:entity`; on success it redirects to `/entities/:entity/:newRowId`; on 400 validation failure it surfaces per-field error messages inline (red border + red help text under the offending field) parsed from the problem+json `errors[]` array. `list_new: "+ New"` button in the list-page header plus a larger CTA in the empty-list state. i18n strings added for both en + ar. No Playwright test yet — that ships with TASK-14.4 when the harness exists. Live smoke-tested against the running stack end-to-end: the rendered page includes the loyalty_tier enum from the L2 metadata overlay, POST creates the customer with `status: "active"` from `lifecycle.initial`. `pnpm verify` green.
- **TASK-10.1b (partial) · Better Auth schema layer** — Migration 0005 adds the `auth` schema with `auth.user`, `auth.session`, `auth.account`, `auth.verification` matching Better Auth 1.6.x column names (camelCase, quoted where reserved) plus `metadata.user_tenant` (multi-tenant authorization mapping) with strict-tenant RLS. `@erp/db` grows Kysely types for the new tables (`AuthUserTable`, `AuthSessionTable`, `AuthAccountTable`, `AuthVerificationTable`, `UserTenantTable`) and a `UserTenantRepository` with `listForUser` / `getForTenant` / `add` / `remove`. Auth tables carry no RLS (platform-global; access gated by Better Auth's session endpoints); `metadata.user_tenant` is strict-tenant. Kysely bumped from 0.27.6 to 0.28.16 to match Better Auth's peer-dep track — no breaking changes in our usage. Every integration test's `freshDb` helper now drops the `auth` schema alongside `metadata` + `ops`. One pre-existing flake fixed: `EntityRowRepository.patchInTx` + `softDeleteInTx` now use `sql\`now()\``for`updated_at`/`deleted_at`instead of JS`new Date()`, eliminating host-vs-Docker clock drift (~100 ms on macOS) that could make patch's `updated_at`appear earlier than create's`DEFAULT now()`. **Better Auth wiring itself (the Fastify plugin + `@erp/auth`package + session-fixture test refactor) is deferred to TASK-10.1b.1** — the first integration attempt hit real upstream snags (shared-Kysely vs. Better Auth's kysely-adapter, schema-qualified table-name routing) that warrant a separate ADR and focused pass. The schema landing here is a clean no-op prerequisite:`pnpm verify`green, 116 integration tests pass (+3 from the 113 baseline because of 2 new migrator tests that now include`user_tenant` and the fixed flake).
- **TASK-10.1a · Zod 4 migration** — Zod bumped from 3.24.2 to 4.3.6 across the workspace catalog; `@asteasolutions/zod-to-openapi` bumped from 7.3.0 to 8.5.0 in both `apps/api` and `apps/kernel` (upstream 8.x peer-depends on `zod@^4.0.0`, confirmed). Three real breakages fixed: (1) `ZodRawShape` is readonly in v4 — `packages/kernel-runtime/src/materialize.ts` now builds `Record<string, ZodTypeAny>` locally and passes to `z.object()`; (2) `z.string().uuid()` now enforces RFC 4122 variant/version nibbles — three synthetic test UUIDs swapped for real v4 UUIDs; (3) `z.infer<z.ZodDiscriminatedUnion>` widens to `unknown` at .d.ts boundaries in Zod 4 — `packages/change-set/src/operations.ts` now exports `type Operation = UpsertOperation | TombstoneOperation` explicitly; `packages/db/src/change-set-repository.ts` parses operations item-by-item so each element keeps the narrow shape at the consumer side. All 14 `z.record()` call sites were already 2-arg (no change). `.superRefine`, `.datetime()`, `.url()`, `.coerce`, `ZodError.issues` — all API-compatible. 113 integration tests across apps/api (58), apps/kernel (10), packages/db (38), packages/events (7) stayed green. `pnpm verify` green. [ADR-0003](./docs/adr/0003-zod-4-migration.md) records the migration; ADR-0002 marked "partly superseded." TASK-10.1b (Better Auth wiring) is the remaining half of TASK-10.1.
- **Phase 2–4 task roadmap** — `docs/phases/` now contains the task queue for everything past Phase 1. `phase-1-cleanup.md` captures the six deferred Phase-1 items (Better Auth / Zod 4 migration, audit-chain backfill, console create-row UI, Grafana Cloud OTLP exporter, Playwright E2E, Terraform + ECS Fargate scaffold). `phase-2-tasks.md` fully decomposes RFC §16.2 into 11 PR-sized tasks (TASK-15 … TASK-25) covering lifecycle guards, full Workflow engine, L1 template layer, Package format + installer, NATS JetStream adapter, L3 V8-isolate scripting, Configuration Studio v0 (read-only), field-level + record-level permissions, online custom-field storage migrator, and AI Specialist v0 (which writes RFC-002 as its first sub-task). `phase-3-tasks.md` and `phase-4-tasks.md` capture RFC §16.3 + §16.4 as epics; those decompose into PR-sized tasks when the preceding phase wraps. `CLAUDE.md` §13 links to this directory. Phase 1 queue stays in CLAUDE.md §13 as a historical record, marked complete.
- **Audit log hash chain (RFC §13.2)** — `@erp/db` ships `AuditRepository` with `appendInTx(trx, input)` writing hash-chained rows inside caller-supplied transactions. Each row's `after_hash = sha256(before_hash || canonicalize(payload))`; per-tenant chains (null-tenant rows form their own vendor chain); a `pg_advisory_xact_lock(hash(tenant_id))` inside the append serializes concurrent writers so the chain never forks. `verifyChain(tenantId)` replays and returns `{audit_pk, reason}` on the first break — catches both tampered diffs (after_hash mismatch) and removed rows (before_hash mismatch). `canonicalize()` and `computeAfterHash()` are exported for offline tamper-detection sweeps. `EntityRowRepository` gains `patchInTx`/`softDeleteInTx` so the Runtime API service can run row mutations and audit appends in one transaction. `RuntimeEntityService.create/patch/delete` wrap the full flow in `withTenantContext()`; every successful `POST/PATCH/DELETE /v1/:entity[/:id]` now writes an audit row with `action = <entity_id>.<verb>`, `target_id = row_id`, diff (`{after}` / `{before,after,changed}` / `{before}`), and context (`{request_id, trace_id}`) — denied/failed requests write nothing. 14 new AuditRepository integration tests + 4 Runtime API audit assertions. Existing ChangeSetRepository audit writes (TASK-07) remain unchained for now; `readLastHashInTx` filters `WHERE after_hash IS NOT NULL` so they don't fork the new chain. Backfill tracked as a separate follow-up.
