# Environment variables consumed by the ERP platform.
# TASK-01 declares no runtime services; this file is populated in TASK-02
# (local dev environment) and expanded by each subsequent task that
# introduces a new env var.
#
# Rule: never commit a real secret. Copy to .env (gitignored) and fill in.
