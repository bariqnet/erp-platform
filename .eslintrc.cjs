// The authoritative ESLint config lives in @erp/config/.eslintrc.cjs.
// This file exists so `eslint .` at the repo root picks up the same rules
// as every workspace package.
module.exports = require("@erp/config/eslintrc");
